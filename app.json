{
    "environments": {
      "test": {
        "scripts": {
          "test": "npm run lint && npm run test -- --watch=false --browsers=ChromeHeadless"
        },
        "buildpacks": [
          { "url": "https://github.com/heroku/heroku-buildpack-google-chrome" },
          { "url": "heroku/nodejs" }
        ]
      }
    }
  }
  
