{
    "environments": {
      "test": {
        "scripts": {
          "test-setup": "npm install",
          "test": "npm run lint && npm run test -- --watch=false --browsers=ChromeHeadles"
        },
        "buildpacks": [
          { "url": "https://github.com/heroku/heroku-buildpack-google-chrome" },
          { "url": "heroku/nodejs" }
        ]
      }
    }
  }
  
